# replay

Replay tests validate checkpoint restoration, tail replay, event ordering, and historical replay semantics without external re-execution.

Every test suite in this directory should map back to `docs/overview/test-strategy.md` and the relevant acceptance criteria.
