# Active phase plans

`meta/current-phase.yaml` is authoritative for the current phase.
All phase plan docs live in this directory so Codex can read future phases without searching multiple locations.

- `PHASE-00` is complete and remains here as historical SSOT.
- Start implementation from the phase listed in `meta/current-phase.yaml`.
