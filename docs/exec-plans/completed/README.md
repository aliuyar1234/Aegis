# Completed phase plans

This directory may hold archived copies of completed phase plans if the project chooses to snapshot them separately later.
Currently, completion state is tracked in `meta/current-phase.yaml` and `meta/phase-gates.yaml`.
