# rs/

Rust owns hot-path sidecars and protocol-heavy adapters.

Use Rust for:
- media/protocol bridges
- recorder/compression sidecars
- binary-heavy adapters
- future voice/media work

Do not use Rust to build a second control plane.
