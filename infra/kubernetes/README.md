# Kubernetes deployment notes

This directory is intentionally documentation-only at the handoff stage.

Planned node pools:
- gateway
- runtime
- projection
- python workers
- rust sidecars

The production topology is described in `ARCHITECTURE.md`.
