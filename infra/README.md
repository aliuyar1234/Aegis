# infra/

Infrastructure scaffolding for Aegis.

## Directories

- `local/` — local development stack
- `kubernetes/` — future deployment manifests and notes
- `helm/` — future packaging notes
- `terraform/` — future infrastructure-as-code notes

The local stack is intentionally real; the other directories document the intended future shape without pretending the implementation already exists.
