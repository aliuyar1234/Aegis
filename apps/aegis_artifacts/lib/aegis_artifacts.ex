defmodule AegisArtifacts do
  @moduledoc """
  Boundary module for artifact metadata, URL issuance, and retention hooks.

  Runtime code may register artifact references, but blob movement and artifact
  lifecycle policy stay in `aegis_artifacts`.
  """
end
